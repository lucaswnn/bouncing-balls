#include "Color.h"

Color::Color(const int red,
             const int green,
             const int blue,
             const int alpha)
    : r(red),
      g(green),
      b(blue),
      a(alpha) {}
